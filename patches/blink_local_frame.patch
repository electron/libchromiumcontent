diff --git a/third_party/WebKit/Source/core/frame/LocalFrame.cpp b/third_party/WebKit/Source/core/frame/LocalFrame.cpp
index 03edc5d..d303b72 100644
--- a/third_party/WebKit/Source/core/frame/LocalFrame.cpp
+++ b/third_party/WebKit/Source/core/frame/LocalFrame.cpp
@@ -427,10 +427,6 @@ void LocalFrame::detach(FrameDetachType type) {
   m_loader.stopAllLoaders();
   m_loader.detach();
   document()->shutdown();
-  // This is the earliest that scripting can be disabled:
-  // - FrameLoader::detach() can fire XHR abort events
-  // - Document::shutdown()'s deferred widget updates can run script.
-  ScriptForbiddenScope forbidScript;
   m_loader.clear();
   if (!client())
     return;
@@ -439,6 +435,10 @@ void LocalFrame::detach(FrameDetachType type) {
   // Notify ScriptController that the frame is closing, since its cleanup ends
   // up calling back to FrameLoaderClient via WindowProxy.
   script().clearForClose();
+  // This is the earliest that scripting can be disabled:
+  // - FrameLoader::detach() can fire XHR abort events
+  // - Document::shutdown()'s deferred widget updates can run script.
+  ScriptForbiddenScope forbidScript;
   setView(nullptr);
 
   m_host->eventHandlerRegistry().didRemoveAllEventHandlers(*domWindow());
