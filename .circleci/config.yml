version: 2.0
jobs:
  build:
    docker:
      - image: electronbuilds/libchromiumcontent:0.0.1
    resource_class: xlarge
    steps:
       - run: script/setup-circle.sh
       - checkout
       - run: script/cibuild
